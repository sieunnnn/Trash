# Spring Data Source ?? - MySQL DB ?? ?? ??

spring:
  datasource:
      url: jdbc:mysql://127.0.0.1:3306/todolist?serverTimezone=Asia/Seoul
      username: root
      password: 123qwe!@#QWE
      driver-class-name: com.mysql.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
        hibernate:
          show_sql: true
          format_sql: true